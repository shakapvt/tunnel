print("Welcome to Tunnel")
print("Press any key + enter to continue whenever you see a ...")
cont = input(str())
print("You awake on a cold hospital cot in an empty operating room ...")
cont = input(str())
print("There is a deafening silence")